[project]
name = "swift-payment-messaging"
version = "0.1.0"
description = "Enterprise SWIFT Payment Messaging Platform"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
]
